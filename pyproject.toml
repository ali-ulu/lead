[project]
name = "leadscout-local-business-scout"
version = "5.0.0"
description = "Local business discovery, multi-source enrichment, CRM, agent API, MCP and social outreach"
requires-python = ">=3.11"
dependencies = [
  "duckdb>=1.1,<2",
  "cryptography>=43,<46",
]

[project.optional-dependencies]
agents = ["mcp>=2,<3"]
