[project]
name = "leadscout-local-business-scout"
version = "4.0.0"
description = "Global local-business discovery, website audits, social enrichment, agent API and MCP"
requires-python = ">=3.11"

[project.optional-dependencies]
agents = ["mcp>=2,<3"]
